export const checkingFrame = (letter, symbol, tictacBoard) => {
    // let board = tictacBoard
    console.log(symbol)
    console.log(tictacBoard)
    for(let i; i <= 2; i++) {
        // if(letter = 'A')
    }
}